# API Security
API_KEY=your-secure-api-key

# Database Configuration
# For local development
DATABASE_URL=sqlite:///./attendance.db
# For production (PostgreSQL example)
# DATABASE_URL=postgresql://user:password@host/dbname

# Google Cloud Storage
GCS_BUCKET_NAME=your-bucket-name
# Uncomment and set path to your service account key file for GCP authentication
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: Set port for the application (default is 8000)
# PORT=8000

# Optional: Set host for the application (default is 127.0.0.1)
# HOST=0.0.0.0